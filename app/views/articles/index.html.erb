<% if @articles.empty? %>

  <p>No article, sorry!</p>

<% else %>

  <ul class="articles-list">

    <% @articles.reverse.each do |article| %>
      <li>
        <h2>
          <%= link_to article.title, article_path(article) %>
        </h2>
      </li>
    <% end %>
  </ul>

<% end %>
