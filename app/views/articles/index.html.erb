<% if @articles.empty? %>

  <p>No artcile, sorry!</p>

<% else %>

  <ul>

    <% @articles.reverse.each do |article| %>
      <li>
        <%= link_to article.title, article_path(article) %>
      </li>
    <% end %>
  </ul>

<% end %>
